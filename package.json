{
  "name": "@whatevertogo/oh-my-kimicli",
  "version": "0.1.3",
  "description": "Personal workflow orchestration for Kimi Code CLI: skills, hooks, persistent continuation, review gates, and usage insights.",
  "type": "module",
  "bin": {
    "omk": "bin/omk.js",
    "oh-my-kimicli": "bin/omk.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/whatevertogo/oh-my-kimicli.git"
  },
  "homepage": "https://github.com/whatevertogo/oh-my-kimicli#readme",
  "bugs": {
    "url": "https://github.com/whatevertogo/oh-my-kimicli/issues"
  },
  "keywords": [
    "kimi",
    "kimi-cli",
    "kimi-code",
    "agent",
    "cli",
    "skills",
    "hooks",
    "workflow",
    "code-review",
    "insights"
  ],
  "files": [
    "bin",
    "lib",
    "plugin",
    "prompts",
    "skills",
    "test",
    "README.md",
    "README_EN.md",
    "LICENSE",
    "tsconfig.json"
  ],
  "scripts": {
    "build:bun": "bun build bin/omk.ts --target=bun --outdir dist/bundle",
    "check": "bun test && bun run typecheck",
    "clean": "bun -e \"import { rmSync, mkdirSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true }); mkdirSync('dist/npm', { recursive: true }); mkdirSync('dist/bun', { recursive: true });\"",
    "pack:all": "bun run check && bun run clean && bun run pack:npm && bun run pack:bun && bun run build:bun",
    "pack:bun": "bun -e \"import { mkdirSync } from 'node:fs'; mkdirSync('dist/bun', { recursive: true });\" && bun pm pack --destination dist/bun",
    "pack:npm": "bun -e \"import { mkdirSync } from 'node:fs'; mkdirSync('dist/npm', { recursive: true });\" && npm pack --pack-destination dist/npm",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "license": "MIT"
}
